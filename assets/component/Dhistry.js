import React from 'react';
import { Text, View } from 'react-native';

const Dhistry = (props) => {
    return (
        <View style={{ backgroundColor: '#fff', marginTop: ts * 2, flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', paddingBottom: ts * 2 }}>
            <View style={{ backgroundColor: '#fff', height: ts * 30, borderRadius: ts * 5, flexDirection: 'row', margin: ts * 2.5 }}>


            </View>


            {/* search bar */}
        </View>
    );
}
export default Dhistry;
